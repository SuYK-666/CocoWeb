# 期末报告_苏禹宽

## 基本信息

- 项目名称：GitHub Project Analyzer
- 个人目录：`成员代码/苏禹宽_GithubProjectAnalyzer`
- 使用工具：Codex
- 模型：codex5.5
- 本次负责环节：开发 / 安全整改 / 文档留痕
- 新增功能：Web 分析任务访问令牌保护

## 一、需求理解

根据《作业三：AI辅助开发过程中的安全管理实践》要求，本次工作不能只修改 README 或前端样式，必须完成一个涉及输入处理、权限判断或数据存取的真实安全功能，并完整记录风险识别、安全约束表达、AI 生成结果检查、问题整改与验证过程。

本次所有修改均限定在 `成员代码/苏禹宽_GithubProjectAnalyzer` 目录下。作业文档要求的 `docs/` 与 `reports/` 目录也按个人目录边界创建为：

- `成员代码/苏禹宽_GithubProjectAnalyzer/docs`
- `成员代码/苏禹宽_GithubProjectAnalyzer/reports`

## 二、风险识别

原 Web 任务流程中，用户创建分析任务后，系统使用 `job_id` 查询运行状态、读取 SSE 实时日志、下载 Markdown/HTML/DOCX 报告。`job_id` 本质上是资源定位标识，不应同时作为访问授权凭据。

如果 `job_id` 出现在浏览器历史、错误日志、截图或他人转发的链接中，第三方可能直接访问以下敏感内容：

- 分析任务实时日志；
- 最终分析报告；
- 报告文件下载地址；
- 仓库结构、依赖、代码洞察、安全风险判断等过程信息。

因此，本次识别出的主要安全风险是：未授权读取任务日志与报告产物。

## 三、安全约束如何进入 AI 交互

我在动手修改前，将风险识别结论转化为结构化约束，并在与 Codex 的交互中持续使用。约束重点如下：

- 每个任务创建时生成独立访问 token；
- `job_id` 只用于定位任务，不能作为唯一访问凭据；
- `/api/result/<job_id>`、`/api/stream/<job_id>`、`/api/download/<job_id>/<fmt>` 三类接口必须统一校验 token；
- 缺失 token 返回 401，错误 token 返回 403；
- token 必须使用安全随机数生成；
- token 比较必须使用常量时间比较；
- 前端可以在当前任务生命周期中保存 token，但不能写入 localStorage；
- 必须新增单元测试覆盖无 token、错 token、正确 token 等情况；
- 禁止修改个人目录以外的任何代码或文档。

完整约束文档同步记录在 `docs/constraint-doc.md`，Prompt 交互记录同步记录在 `docs/prompt-records.md`。

## 四、关键 Prompt 的完整文本

以下 Prompt 是根据作业要求整理后的规范化上交版本，用于体现安全约束如何进入 AI 交互。它不是简单记录口语化任务描述，而是把背景、任务范围、约束条件和禁止行为明确写入同一次 AI 编程请求中，避免模型在实现过程中遗漏安全边界。

### Prompt 1：项目背景与工作边界

```text
背景说明：
我正在完成信息安全管理课程的 AI 辅助开发实践作业。当前项目是 GitHub Project Analyzer，一个基于 Flask 的本地 Web 工具。用户提交 DeepSeek API Key、GitHub 仓库地址和分析方向后，系统异步生成 Markdown、HTML、DOCX 三种分析报告。

任务范围：
请只在 成员代码/苏禹宽_GithubProjectAnalyzer 目录内进行代码修改和文件新增。作业要求中的 docs/、reports/、测试文件和期末报告都必须放在该个人目录内，不能修改其他成员目录或仓库根目录中的无关文件。

目标：
选择一个与信息安全直接相关的功能点进行实质性补强，要求功能可以运行，并能通过测试或验证证明确实解决了安全问题。优先选择涉及访问控制、输入处理、权限判断、数据读取或错误响应的模块。

交付物：
1. 完成安全功能代码实现；
2. 增加必要的测试或验证脚本；
3. 在个人目录下补充 docs/risk-analysis.md、docs/constraint-doc.md、docs/prompt-records.md、docs/security-checklist.md、docs/fix-report.md；
4. 在个人目录下补充 reports/scan-report.md 和 reports/before-after-diff.md；
5. 在个人目录下生成 期末报告_苏禹宽.md，详细记录风险识别、Prompt、安全代码片段、偏差修正和验证结果。
```

### Prompt 2：安全功能实现约束

```text
请对 Web 分析任务结果访问流程进行安全加固。

风险背景：
当前系统通过 job_id 查询任务状态、读取 SSE 日志和下载报告。job_id 是资源定位标识，如果它同时作为访问凭据，一旦泄露，第三方可能直接读取分析日志和报告产物。

要求：
1. 每个分析任务创建时生成独立 access token；
2. job_id 只用于定位任务，不能作为唯一访问凭据；
3. /api/result/<job_id> 必须校验 access token；
4. /api/stream/<job_id> 必须校验 access token；
5. /api/download/<job_id>/<fmt> 必须校验 access token；
6. 缺少 token 时返回 401；
7. token 错误时返回 403；
8. token 必须使用安全随机数生成，不能由时间戳、仓库名、用户名或递增序列拼接得到；
9. token 比较必须使用常量时间比较；
10. 前端需要自动携带 token，保证合法用户仍能查看进度、轮询状态和下载报告；
11. 前端不得把任务 access token 写入 localStorage；
12. 不得记录或提交真实 DeepSeek API Key、GitHub Token 或其他敏感凭据。

测试要求：
请新增单元测试，至少覆盖以下场景：
1. 不带 token 访问任务结果应被拒绝；
2. 使用错误 token 访问任务结果应被拒绝；
3. 使用正确 token 访问任务结果应成功；
4. 下载接口必须先执行授权检查。
```

### Prompt 3：AI 生成结果安全审查与整改要求

```text
请对刚才生成的代码进行安全审查，重点检查：
1. 安全约束是否落实在实际代码逻辑中，而不只是写在注释或文档中；
2. 三个敏感接口 result、stream、download 是否都调用了同一套授权校验；
3. token 是否使用 secrets 等安全随机源生成；
4. token 比较是否使用常量时间比较；
5. 前端是否只在当前运行状态中保存任务 token，而不是写入 localStorage；
6. 下载链接、SSE 链接、轮询链接是否都能携带 token；
7. 新增测试是否能证明未授权访问被拒绝、合法访问仍可用。

如果发现偏差，请记录问题是什么、如何修正、修正后应满足什么条件，并把结果写入 docs/fix-report.md、reports/before-after-diff.md 和 期末报告_苏禹宽.md。
```

## 五、AI 生成结果中与安全约束相关的关键片段

### 1. 使用安全随机数生成任务访问令牌

```python
access_token = secrets.token_urlsafe(32)
```

安全意义：令牌由 Python 标准库 `secrets` 生成，不依赖时间戳、仓库名、用户名或可预测序列，降低猜测风险。

### 2. 从请求中读取访问令牌

```python
def _get_request_token() -> str:
    auth_header = request.headers.get("Authorization", "")
    if auth_header.lower().startswith("bearer "):
        return auth_header.split(" ", 1)[1].strip()
    return str(request.args.get("token", "")).strip()
```

安全意义：同时支持 `Authorization: Bearer` 与查询参数 `token`。其中查询参数用于浏览器原生 EventSource 和下载链接场景，Bearer 方式为后续 API 调用保留更规范的扩展路径。

### 3. 统一授权校验与常量时间比较

```python
def _authorize_job(job_id: str) -> dict[str, Any]:
    job = _get_job(job_id)
    if not job:
        abort(404, description="job not found")

    expected_token = str(job.get("access_token", ""))
    provided_token = _get_request_token()
    if not expected_token or not provided_token:
        abort(401, description="missing job access token")
    if not secrets.compare_digest(expected_token, provided_token):
        abort(403, description="invalid job access token")
    return job
```

安全意义：将授权判断集中到一个函数中，避免某个敏感接口漏掉检查；`compare_digest` 用于降低普通字符串比较可能带来的时序侧信道风险。

### 4. 三类敏感接口统一调用授权校验

```python
@app.get("/api/stream/<job_id>")
def stream(job_id: str) -> Response:
    job = _authorize_job(job_id)
```

```python
@app.get("/api/result/<job_id>")
def result(job_id: str) -> Response:
    _authorize_job(job_id)
```

```python
@app.get("/api/download/<job_id>/<fmt>")
def download(job_id: str, fmt: str) -> Response:
    _authorize_job(job_id)
```

安全意义：实时日志、结果查询、报告下载都属于任务敏感数据读取，必须具有同一安全边界。

### 5. 前端自动携带 token，但不持久化任务 token

```javascript
const state = {
    currentStep: 0,
    eventSource: null,
    resultUrl: "",
    accessToken: "",
    jobId: "",
    logCursor: 0,
    pollTimer: null,
    finalResolved: false,
    streamWarned: false,
};
```

```javascript
response = await fetch(withQuery(state.resultUrl, { t: Date.now(), token: state.accessToken }), {
    cache: "no-store",
    headers: { "Cache-Control": "no-cache" },
});
```

安全意义：任务访问 token 只保存在本次页面运行状态中，没有写入 localStorage；轮询时自动携带 token，保证新增授权逻辑不会破坏正常功能。

## 六、发现偏差或问题时的交互记录

### 偏差 1：下载链接需要同步携带 token

发现情况：后端给下载接口增加 `_authorize_job` 后，如果最终返回给前端的 `downloadUrls` 不带 token，用户点击下载会被新增授权逻辑拦截。

修正动作：在 `_run_analysis_job` 构造 `downloadUrls` 时追加当前任务的 `job_access_token`。

```python
downloadUrls = {
    fmt: f"/api/download/{job_id}/{fmt}?token={job_access_token}"
    for fmt in ("docx", "html", "md")
    if fmt in report_paths
}
```

修正结果：报告生成完成后，合法用户仍可下载 DOCX/HTML/MD，未携带 token 的请求仍会被拒绝。

### 偏差 2：轮询 URL 已有查询参数时不能再简单拼接 `?t=...`

发现情况：后端返回的 `resultUrl` 已包含 `?token=...`，如果前端继续使用原逻辑 `${state.resultUrl}?t=${Date.now()}`，会形成错误查询串。

修正动作：新增 `withQuery` 函数统一合并查询参数。

```javascript
function withQuery(url, params) {
    const target = new URL(url, window.location.origin);
    for (const [key, value] of Object.entries(params)) {
        if (value !== undefined && value !== null && String(value) !== "") {
            target.searchParams.set(key, String(value));
        }
    }
    return `${target.pathname}${target.search}`;
}
```

修正结果：轮询、SSE 与下载场景都可以稳定携带 token，不会破坏已有参数。

### 偏差 3：单元测试导入被第三方 `tests` 包抢占

发现情况：第一次运行 `python -m unittest tests.test_job_access_control -v` 时，当前 Python 环境中存在第三方 `tests` 包，导致本地测试模块没有被正确导入。

修正动作：在个人项目目录的 `tests/` 下新增 `__init__.py`，明确本地测试包边界。

修正结果：再次运行单元测试，4 个授权测试全部通过。

## 七、代码补强操作记录

1. 克隆个人 fork `SuYK-666/CocoWeb` 到当前目录，并确认当前分支为 `main`。
2. 阅读作业 PDF，确认每位成员需要单独 PR，且过程材料需进入 GitHub 仓库。
3. 阅读 `webapp.py`、`web/static/app.js`、`README.md` 和项目结构，确定 Web 任务访问控制为本次具体功能。
4. 修改 `webapp.py`，引入 `secrets` 并在 `_create_job` 中生成 `access_token`。
5. 新增 `_get_request_token`，从 Bearer Header 或查询参数读取 token。
6. 新增 `_authorize_job`，统一处理任务不存在、缺失 token、错误 token 三类情况。
7. 在 `/api/stream/<job_id>`、`/api/result/<job_id>`、`/api/download/<job_id>/<fmt>` 中加入授权校验。
8. 修改任务创建响应，返回 `accessToken`，并把 token 写入 `streamUrl`、`resultUrl`。
9. 修改报告生成完成后的下载 URL，确保下载链接也携带 token。
10. 修改 `web/static/app.js`，新增 `state.accessToken` 和 `withQuery`。
11. 修改前端轮询、SSE 连接与下载逻辑，使其自动携带 token。
12. 新增 `tests/test_job_access_control.py`，验证缺失 token、错误 token、正确 token 与下载接口授权前置检查。
13. 新增 `tests/__init__.py`，解决测试导入冲突。
14. 新增 `docs/risk-analysis.md`、`docs/constraint-doc.md`、`docs/prompt-records.md`、`docs/security-checklist.md`、`docs/fix-report.md`。
15. 新增 `reports/scan-report.md` 和 `reports/before-after-diff.md`。
16. 根据反馈补充本报告，使 Prompt、关键安全片段、偏差交互记录直接体现在报告正文中。

## 八、验证情况

```text
python -m compileall webapp.py web\static\app.js tests
结果：通过

python -m unittest tests.test_job_access_control -v
结果：4 项测试全部通过
```

测试覆盖点：

- 缺失 token 访问 `/api/result/<job_id>` 返回 401；
- 错误 token 访问 `/api/result/<job_id>` 返回 403；
- 正确 token 访问 `/api/result/<job_id>` 返回 200；
- 下载接口在任务状态检查前先执行授权校验。

## 九、安全审查结论

本次改动将任务定位与访问授权分离，避免 `job_id` 单独承担访问凭据职责。实时日志、结果查询、报告下载三个敏感入口均统一执行授权校验。前端仅在当前任务运行态保存 token，没有额外持久化任务访问令牌。新增测试证明未授权请求会被拒绝，合法请求仍可正常读取任务状态，满足作业中“能运行起来，并且解决了某安全问题”的要求。

## 十、过程材料位置

- 风险分析：`docs/risk-analysis.md`
- 项目约束：`docs/constraint-doc.md`
- Prompt 记录：`docs/prompt-records.md`
- 安全清单：`docs/security-checklist.md`
- 整改说明：`docs/fix-report.md`
- 扫描报告：`reports/scan-report.md`
- 前后对比：`reports/before-after-diff.md`
